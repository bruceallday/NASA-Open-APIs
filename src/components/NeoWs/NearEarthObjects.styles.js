import { makeStyles, useTheme } from "@material-ui/core/styles"

export const useStyles = makeStyles(themes => ({
         root: {
           marginTop: 100,
           width: '80vw',
           marginLeft: 70,
         }
       }));